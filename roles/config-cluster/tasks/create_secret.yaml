#Creating Docker registry secret to store docker hub credentials
apiVersion: v1
kind: Secret
metadata:
  name: regcred
  namespace: api
data:
  .dockerconfigjson: YOUR_BASE64_DOCKER_CONFIG.JSON_FILE
type: kubernetes.io/dockerconfigjson